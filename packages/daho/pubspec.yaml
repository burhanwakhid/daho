name: daho
description: A fast, minimal HTTP framework for Dart backed by a native H2O server over FFI, with an Express/Fiber-style API and multi-core support.
version: 0.1.0
# repository: https://github.com/my_org/daho
resolution: workspace
environment:
  sdk: ^3.9.2

dependencies:
  ffi: ^2.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
