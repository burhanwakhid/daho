<!DOCTYPE html>
<html>
<head><title>{{ $title ?? 'Welcome' }}</title></head>
<body>
    <h1>{{ $title ?? 'Welcome to Daho + Clurit!' }}</h1>

    @if($users != null && $users.isNotEmpty)
        <ul>
            @foreach($users as $user)
                <li>{{ $user }}</li>
            @endforeach
        </ul>
    @else
        <p>No users found.</p>
    @endif
</body>
</html>
