name: _daho_workspace
publish_to: none

environment:
  sdk: ^3.9.2

# Pub workspace (monorepo). Members share a single lockfile.
workspace:
  - packages/daho
  - packages/daho_cli
  - packages/daho_auth
  - packages/clurit
  - packages/clurit_daho
